var config = require('./config');
var sites = require('./config');

module.exports = {
/*

  pull :: [string] -> promise
  pull(paths)

  pullAll :: -> promise
  pullAll()

  push :: [string] -> object? -> promise
  push(files, [{overwrite}])

  pushAll :: object? -> promise
  pushAll([{overwrite}])

  add :: [string] -> promise
  add(files)

  remove :: [string] -> promise
  remove(files)

*/
}

